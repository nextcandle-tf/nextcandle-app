# NextCandle App

The mobile application for NextCandle, built with Flutter.

## 📱 Overview
This project targets Android and iOS platforms, providing a native mobile experience for NextCandle users. It integrates with the NextCandle API and wraps specific web functionalities alongside native features.

## 🛠️ Status
*   **Framework**: Flutter
*   **Current State**: Repository initialized. awaiting project skeleton.

## 🚀 Getting Started
1.  Ensure you have the Flutter SDK installed.
2.  Clone the repository:
    ```bash
    git clone https://github.com/nextcandle-tf/nextcandle-app.git
    ```
3.  Install dependencies:
    ```bash
    flutter pub get
    ```
4.  Run on device/emulator:
    ```bash
    flutter run
    ```
